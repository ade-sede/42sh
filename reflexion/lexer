struct s_action_table g_action_table[]
{
	{STATE, function};
	{NULL, NULL};
}

/*
**
/*
t_list *lex_one_token_le_oriented(t_lexer *lex)
{
	delim_token
	return (tokenize_le);
}

lex_one_token_exec_oriented(t_lexer *lex)
{
	delim_token
	if (*state = DEF)
		tokenize_full;
	else
		reopen
}

// SUBGESTION : reverse stack to smooth operations in most cases.
