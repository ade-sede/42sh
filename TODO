ls |& -> parse error -> si il reste des token


ls | -> right branch contient que simple command avec pas de fils


ls | 2>1 sans command name


ajouter un espace entre les entree dans l historique


BUGS:

ls |
cat -e

ls ; // fait un retour erreur
